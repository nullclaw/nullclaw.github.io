# NullClaw Documentation
 
Source code for the SvelteKit documentation site hosted at `<https://nullclaw.github.io>`.

## Development

```bash
npm install
npm run dev
```

## Deployment

The site is configured to deploy automatically via GitHub Actions.
Make sure the repository settings for **Pages** have the **Source** set to **GitHub Actions**.
